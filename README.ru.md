# WireGuard Tray

[![English](https://img.shields.io/badge/lang-English-blue)](README.md)
[![Русский](https://img.shields.io/badge/lang-Русский-red)](README.ru.md)

Легковесное приложение для системного трея для управления VPN-подключениями WireGuard на Linux.

## Возможности

- 📌 **Интеграция в системный трей** - Легкий доступ из панели рабочего стола
- 🔄 **Подключение/отключение в один клик** - Переключайте VPN одним нажатием
- 🎯 **Поддержка нескольких интерфейсов** - Переключение между разными конфигурациями WireGuard
- 🔔 **Уведомления на рабочем столе** - Получайте уведомления об изменении статуса подключения
- ⚡ **Быстрый доступ через CLI** - Командный интерфейс для автоматизации
- 🔧 **Автонастройка sudoers** - Легкая настройка управления VPN без пароля
- 🚀 **Автозапуск через Systemd** - Запуск при старте системы

## Скриншоты

![Иконка в трее](icon-on.png) *Активное подключение*
![Иконка в трее](icon-off.png) *Неактивное подключение*

## Установка

### Из AUR (Arch Linux)

```bash
# Используя yay
yay -S wireguard-tray

# Используя paru
paru -S wireguard-tray
```

### Ручная установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш_username/wireguard-tray.git
   cd wireguard-tray
   ```

2. Соберите и установите:
   ```bash
   makepkg -si
   ```

## Настройка

### 1. Настройка Sudoers

Для запуска WireGuard без ввода пароля каждый раз:

```bash
sudo wireguard-tray --sudo-setup
```

Это создаст файл `/etc/sudoers.d/wireguard-tray` с соответствующими разрешениями.

### 2. Конфигурация WireGuard

Разместите ваши конфигурационные файлы WireGuard в `/etc/wireguard/`:
```bash
sudo nano /etc/wireguard/wg0.conf
```

Пример конфигурации:
```ini
[Interface]
PrivateKey = ВАШ_ПРИВАТНЫЙ_КЛЮЧ
Address = 10.0.0.2/24
DNS = 1.1.1.1

[Peer]
PublicKey = ПУБЛИЧНЫЙ_КЛЮЧ_СЕРВЕРА
AllowedIPs = 0.0.0.0/0
Endpoint = vpn.server.com:51820
```

### 3. Автозапуск (Опционально)

Включите автоматический запуск с systemd:
```bash
wireguard-tray --autostart
```

Отключите автозапуск:
```bash
wireguard-tray --disable-autostart
```

## Использование

### Графический интерфейс

1. Запустите приложение:
   ```bash
   wireguard-tray
   ```

2. Нажмите на иконку в трее, чтобы увидеть меню:
   - **VPN** чекбокс - Включить/выключить подключение
   - **Интерфейс** - Выбрать конфигурацию WireGuard
   - **Выход** - Закрыть приложение

### Командный интерфейс

```bash
# Показать справку
wireguard-tray --help

# Подключиться к определенному интерфейсу
wireguard-tray --up wg0

# Отключить текущий интерфейс
wireguard-tray --down

# Установить интерфейс по умолчанию
wireguard-tray --set wg1

# Включить автозапуск
wireguard-tray --autostart

# Выключить автозапуск
wireguard-tray --disable-autostart

# Настроить sudoers (запускать с sudo)
sudo wireguard-tray --sudo-setup

# Показать версию
wireguard-tray --version
```

## Структура проекта

```
wireguard-tray/
├── tray.py                    # Основной скрипт приложения
├── icon-on.png               # Иконка активного подключения
├── icon-off.png              # Иконка неактивного подключения
├── wireguard-tray.desktop   # Файл для автозапуска в DE
├── wireguard-tray.service   # Systemd сервис
├── wireguard-tray.install   # Скрипт пост-установки
├── PKGBUILD                 # Файл сборки пакета для Arch Linux
└── README.md                # Этот файл
```

## Зависимости

- **Python 3.x**
- **GTK 3** и Python bindings
- **libappindicator** для интеграции в системный трей
- **WireGuard Tools** (`wg-quick`)
- **libnotify** для уведомлений на рабочем столе

Установите зависимости на Arch Linux:
```bash
sudo pacman -S python python-gobject gtk3 libappindicator-gtk3 wireguard-tools libnotify
```

## Устранение неполадок

### Распространенные проблемы

1. **"Permission denied" при подключении**
   ```bash
   sudo wireguard-tray --sudo-setup
   ```

2. **Интерфейсы не найдены в меню**
   Убедитесь, что конфигурации WireGuard находятся в `/etc/wireguard/` с расширением `.conf`:
   ```bash
   sudo chmod 755 /etc/wireguard/
   ls -la /etc/wireguard/*.conf
   ```

3. **Иконка в трее не отображается**
   Проверьте, поддерживает ли ваша среда рабочего стола AppIndicator:
   ```bash
   # Для KDE Plasma
   sudo pacman -S plasma5-applets-appindicator
   
   # Для GNOME
   sudo pacman -S gnome-shell-extension-appindicator
   ```

4. **Уведомления не работают**
   Убедитесь, что демон уведомлений запущен:
   ```bash
   systemctl --user status dunst  # или любой другой демон уведомлений
   ```

### Режим отладки

Запустите с подробным выводом:
```bash
python3 tray.py
```

Проверьте логи systemd:
```bash
journalctl --user -u wireguard-tray -f
```

## Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для функции: `git checkout -b имя-функции`
3. Зафиксируйте изменения: `git commit -am 'Добавлена функция'`
4. Запушьте в ветку: `git push origin имя-функции`
5. Создайте Pull Request

## Лицензия

MIT License - смотрите файл [LICENSE](LICENSE) для подробностей.

## Поддержка

- **Issues**: [GitHub Issues](https://github.com/ваш_username/wireguard-tray/issues)
- **Email**: ваш.email@example.com

## Благодарности

- WireGuard® является зарегистрированной торговой маркой Jason A. Donenfeld
- Иконки сделаны [Freepik](https://www.flaticon.com) с [Flaticon](https://www.flaticon.com)

---

**Примечание**: Это приложение не является официально аффилированным с проектом WireGuard.